<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <!--<item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
         <item name="colorAccent">@color/colorAccent</item>
        <item name="colorButtonNormal">@color/myBlue</item>
         <item name="colorControlHighlight">@color/myWhite</item>-->
    </style>
    <style name="myColouredButton" parent="Base.Widget.AppCompat.Button.Colored">
        <item name="colorButtonNormal">@color/myPeach</item>
        <item name="colorControlHighlight">@color/myRed</item>
    </style>
    <style name="myBorderButton" parent="Base.Widget.AppCompat.Button.Borderless.Colored">
        <item name="colorButtonNormal">@color/pureBlack</item>
        <item name="colorControlHighlight">@color/myPeach</item>
        <item name="android:textColor">@color/pureWhite</item>

    </style>
</resources>
